apply plugin: 'com.android.library'

ext {
    bintrayName = 'file-x5'
    artifact = bintrayName
    libraryName = 'file-x5'
    libraryDescription = 'file load for android by x5'
    libraryVersion = pdf_load_version
}

android {
    compileSdkVersion 29
    buildToolsVersion "29.0.3"

    defaultConfig {
        minSdkVersion 19
        targetSdkVersion 26
        versionCode 1020
        versionName "1.0.2"
    }

    buildTypes {
        release {
            debuggable false
            minifyEnabled false
        }
    }
}

dependencies {
    compileOnly project(path:':web')

}

apply from: '../install.gradle'
apply from: '../bintray.gradle'